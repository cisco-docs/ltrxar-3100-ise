# IP SGT Mapping

*Location in GUI*:
`Work Centers` » `TrustSec` » `Components` » `IP SGT Static Mapping`

{{ doc_gen }}

### Examples

```yaml
ise:
  trust_sec:
    ip_sgt_mappings:
      - host_ip: 10.0.0.1/32
        deploy_type: ALL
        sgt: Sgt97
```
