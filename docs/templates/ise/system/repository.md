# Repository

*Location in GUI*:I**:I**:
`Administration` » `System` » `Maintenance` » `Repository`

{{ doc_gen }}

### Examples

```yaml
ise:
  system:
    repositories:
      - name: "LocalDisk"
        protocol: DISK
        path: "/"
```
